#include "main.h"
/**
 * _abs - Program to compute absolute value of integer
 * @n: the int to check
 * Return: thr absolute va;ue of int
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
		return (-n);
}
